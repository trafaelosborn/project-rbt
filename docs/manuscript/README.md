# Manuscript Draft

This folder contains the LaTeX draft for the first Project RBT paper based on
the completed French -> Latin `v5` paper run.

Primary file:

- `french_to_latin_retrodiction.tex`

Suggested build command if a TeX distribution is installed:

```powershell
cd "c:\Code\Project RBT\project_rbt\docs\manuscript"
pdflatex french_to_latin_retrodiction.tex
```

Current status:

- Draft written from in-repo results and reports
- No bibliography file yet
- No journal-specific formatting yet
- Author metadata still placeholder
